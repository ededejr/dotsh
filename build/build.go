package build

var (
	// Version is the current version of the CLI
	Version string
	// Commit is the current commit of the CLI
	Commit string
	// Date is the date the CLI was built
	Date string
	// User is the user who built the CLI
	User string
	// BuiltBy is the machine that built the CLI
	BuiltBy string
)
